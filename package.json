{
  "name": "echo-arena",
  "version": "0.3.0",
  "private": true,
  "description": "ECHO - arena espectral solo e multiplayer local",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "test": "node --test",
    "check": "node --check game.js && node --check shared/simulation.js && node --check server/database.js && node --check server/multiplayer.js && node --check server/index.js"
  },
  "engines": {
    "node": ">=24.15.0"
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
