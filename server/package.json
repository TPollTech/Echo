{
  "name": "echo-arena-server",
  "version": "0.7.0",
  "private": true,
  "description": "ECHO multiplayer server — Railway deployment",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "migrate": "node ../scripts/migrate.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "ws": "^8.21.1",
    "pg": "^8.13.0",
    "@upstash/redis": "^1.34.0"
  }
}
